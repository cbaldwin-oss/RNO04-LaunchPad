# Secrets — NEVER commit
.streamlit/secrets.toml
secrets.toml
*.env

# Local SQLite store — rebuilt fresh by the refresh Action; keeps client data out of git
dashboard_data.db

# Self-contained snapshot build (embeds client data — keep out of git)
dashboard-snapshot*.html
vendor/plotly-*.min.js

# Python
__pycache__/
*.pyc
venv/
.venv/
